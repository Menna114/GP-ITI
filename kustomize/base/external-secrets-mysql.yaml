apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: mysql-credentials3
  namespace: default
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager2
    kind: SecretStore
  target:
    name: mysql-credentials3
    creationPolicy: Owner
  data:
    - secretKey: MYSQL_ROOT_PASSWORD
      remoteRef:
        key: mysql-credentials3
        property: MYSQL_ROOT_PASSWORD
    - secretKey: hostname
      remoteRef:
        key: mysql-credentials3
        property: hostname
    - secretKey: username
      remoteRef:
        key: mysql-credentials3
        property: username
    - secretKey: password
      remoteRef:
        key: mysql-credentials3
        property: password
    - secretKey: port
      remoteRef:
        key: mysql-credentials3
        property: port
